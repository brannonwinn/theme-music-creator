# ===================================================================
# Observability Configuration Template
# Copy to .env and customize for your project/worktree
# ===================================================================

# === REQUIRED ===
# These variables are validated by the pre_tool_use hook
# Operations will be BLOCKED if any of these are missing or empty
# Run: ./.claude/scripts/sync_worktree.sh --interactive to fix issues

# Project identification (must match across all worktrees)
PROJECT_NAME=agent_observer

# Observability API endpoint (centralized server)
OBSERVABILITY_API_URL=http://localhost:6789

# Shared Supabase Redis on port 6380, database 0
# This is the shared Redis instance available to all projects
REDIS_URL=redis://localhost:6380/0

# === OPTIONAL (Auto-detected if not set) ===

# Worktree identification
# Auto-detected from path (agents/agent_blue -> blue)
# Set explicitly to override auto-detection
#WORKTREE_NAME=blue
#AGENT_COLOR=blue
#AGENT_NAME=Blue Guardian

# Git repository name (auto-detected from git remote)
#GIT_REPO_NAME=agent_observer

# Frontend dev server (only needed during frontend development)
# In production, frontend is served by API on same port
#FRONTEND_DEV_URL=http://localhost:6790

# === DETECTION BEHAVIOR ===
#
# Priority order for agent identification:
# 1. AGENT_COLOR env var (highest priority)
# 2. Auto-detect from cwd path (agents/agent_* or worktrees/*)
# 3. Main agent (no worktree)
#
# Examples:
#
# Worktree setup:
#   /path/to/project/agents/agent_blue/.env
#   PROJECT_NAME=myproject
#   AGENT_COLOR=blue
#   AGENT_NAME=Blue Guardian
#
# Main project:
#   /path/to/project/.env
#   PROJECT_NAME=myproject
#   (no worktree vars = main agent)
#
# - `BACKEND_DIR` - Backend directory relative to project root (default: `app`)
#   - Use when your project uses a different backend directory name
#   - Examples: `backend`, `server`, `api`, `src`, `packages/api`
# - `FRONTEND_DIR` - Frontend directory relative to project root (default: `frontend`)
#   - Use when your project uses a different frontend directory name
#   - Examples: `client`, `web`, `ui`, `dashboard`, `apps/web`

# **When to set these:**
# - When porting worktree system to projects with non-standard directory structures
# - For monorepo setups with nested directories
# - When the backend is not in `./app` or frontend is not in `./frontend`

# **Examples:**

# ```bash
# # Standard Next.js project structure
# BACKEND_DIR=backend
# FRONTEND_DIR=client

# # Monorepo with packages
# BACKEND_DIR=packages/api
# FRONTEND_DIR=apps/dashboard

# # Different naming convention
#BACKEND_DIR=server
#FRONTEND_DIR=web






# === SOURCE APP (Legacy) ===
# Used for backward compatibility with existing hooks
# Will be replaced by PROJECT_NAME in the future
SOURCE_APP=${PROJECT_NAME}

# === DEVELOPER-SPECIFIC SETTINGS ===
# These settings are personal to each developer and should NOT be committed

# Developer name (used in TTS notifications, 50% chance to include)
#ENGINEER_NAME=YourName

# === TTS (Text-to-Speech) Configuration ===
# Priority order when multiple are enabled: ElevenLabs > OpenAI > pyttsx3
# If a service fails, it automatically falls back to the next enabled service

# Enable/disable TTS services (set to true/false)
ENABLE_ELEVENLABS_TTS=false
ENABLE_OPENAI_TTS=false
ENABLE_PYTTSX3_TTS=false

# API Keys (required if respective service is enabled)
#ELEVENLABS_API_KEY=sk_your_elevenlabs_key_here
#OPENAI_API_KEY=sk-your_openai_key_here
#ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here
